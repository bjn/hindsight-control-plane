name: Build and deploy Hindsight Control Plane

on:
  push:
    branches: [main]
    paths:
      - Dockerfile
      - docker-compose.yml
      - .github/workflows/build-image.yml
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/bjn/hindsight-control-plane
  COOLIFY_API_URL: https://app.coolify.io/api/v1
  COOLIFY_APPLICATION_UUID: REPLACE_COOLIFY_APPLICATION_UUID

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=long
      - uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          pull: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Trigger Coolify deploy
        if: github.ref == 'refs/heads/main' && env.COOLIFY_APPLICATION_UUID != 'REPLACE_COOLIFY_APPLICATION_UUID'
        env:
          COOLIFY_API_KEY: ${{ secrets.COOLIFY_API_KEY }}
        run: |
          test -n "$COOLIFY_API_KEY"
          curl --fail --show-error --silent \
            --get "${COOLIFY_API_URL}/deploy" \
            --header "Authorization: Bearer ${COOLIFY_API_KEY}" \
            --data-urlencode "uuid=${COOLIFY_APPLICATION_UUID}" \
            --data-urlencode "force=false"
