{
  "name": "hindsight-explorer",
  "version": "1.0.0",
  "private": true,
  "description": "Read-only browser explorer for self-hosted Hindsight memory banks.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=22"
  }
}
